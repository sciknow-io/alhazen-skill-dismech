name: gene_descriptor
description: Map dismech genedescriptor entities to dm-gene-descriptor
depends_on: []
idempotent: true
source_match: |
  match
    $x isa genedescriptor, has preferred-term $pt;
  fetch {
    "preferred_term": $pt
  };
target_insert: |
  insert
    $td isa dm-gene-descriptor,
      has id $skolem_id,
      has name $preferred_term,
      has dm-preferred-term $preferred_term;
skolem_keys: [preferred_term]
