name: disease_descriptor
description: Map dismech diseasedescriptor entities to dm-disease-descriptor
depends_on: []
idempotent: true
source_match: |
  match
    $x isa diseasedescriptor, has preferred-term $pt;
  fetch {
    "preferred_term": $pt
  };
target_insert: |
  insert
    $td isa dm-disease-descriptor,
      has id $skolem_id,
      has name $preferred_term,
      has dm-preferred-term $preferred_term;
skolem_keys: [preferred_term]
