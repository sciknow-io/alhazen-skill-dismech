name: process_descriptor
description: Map dismech biologicalprocessdescriptor to dm-process-descriptor
depends_on: []
idempotent: true
source_match: |
  match
    $x isa biologicalprocessdescriptor, has preferred-term $pt;
  fetch {
    "preferred_term": $pt
  };
target_insert: |
  insert
    $td isa dm-process-descriptor,
      has id $skolem_id,
      has name $preferred_term,
      has dm-preferred-term $preferred_term;
skolem_keys: [preferred_term]
